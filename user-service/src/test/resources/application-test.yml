spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  liquibase:
    enabled: false
  kafka:
    listener:
      auto-startup: false
  jpa:
    hibernate:
      ddl-auto: none

springdoc:
  swagger-ui:
    enabled: false